---
import { Image } from "astro:assets";
import tacoronte from "~/assets/tacoronte_bg.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="h-full w-full object-cover"
  src={tacoronte}
  widths={widths}
  sizes={sizes}
  alt="The ridged surface of the moon"
/>
